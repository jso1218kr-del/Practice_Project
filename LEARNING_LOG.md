# 학습 기록

이 파일은 코딩 에이전트의 보고서가 아니라 **직접 관찰한 내용을 적는 노트**입니다.
정답처럼 쓰려고 하기보다 실행 전 예상과 실행 후 결과의 차이를 기록해 보세요.

## 현재 단계

- 1단계: 최소 실행 가능한 비교 거래 기준선
- 내가 이 단계에서 만들고 있다고 이해한 것:
  - 

## 이번 단계에서 사용한 기술

- Python 3.12
- pandas
- CSV
- Haversine 거리 공식
- 중앙값과 분위수
- pytest와 Ruff

가장 낯선 기술 하나와 그 이유:

- 

## 이해 확인 질문 (10개)

코드를 수정하는 실습 대신, 아래 질문에 자신의 말로 답하면서 1단계의 기술과 전체 흐름을
점검합니다. 답은 먼저 제공하지 않고, 작성한 답을 바탕으로 함께 확인합니다.

1. Python은 이 프로젝트에서 어떤 역할을 하며 React, Spring, Flutter와 무엇이 다른가?
   - 내 답:
2. CSV 파일을 pandas `DataFrame`으로 읽는 이유는 무엇인가?
   - 내 답:
3. 입력 주소의 위도와 경도는 현재 어떤 방식으로 찾으며, 실제 주소 API와 무엇이 다른가?
   - 내 답:
4. 위도·경도를 단순히 빼지 않고 Haversine 공식을 사용하는 이유는 무엇인가?
   - 내 답:
5. `find_comparables()`가 거래를 선택할 때 적용하는 조건을 모두 설명할 수 있는가?
   - 내 답:
6. 평가 기준일 이후 거래와 취소 거래를 제외해야 하는 이유는 무엇인가?
   - 내 답:
7. 총 거래가격을 바로 비교하지 않고 ㎡당 가격을 먼저 계산하는 이유는 무엇인가?
   - 내 답:
8. 평균 대신 중앙값을 사용하면 어떤 거래가격의 영향을 줄일 수 있는가?
   - 내 답:
9. 20분위수와 80분위수는 가격 판정에서 각각 어떤 역할을 하는가?
   - 내 답:
10. 비교 거래가 3건 미만일 때 계산 가능한 값이 있어도 `INSUFFICIENT_DATA`를 반환하는
    이유는 무엇인가?
    - 내 답:

## 직접 확인할 코드

- [ ] `distance.py`에서 각도를 라디안으로 바꾸는 부분
- [ ] `comparable_search.py`의 필터 조건을 하나씩 설명해 보기
- [ ] `evaluator.py`에서 비교 거래 3건 미만일 때 일찍 반환하는 부분
- 추가로 궁금했던 코드:
  - 

## 실행 명령

```powershell
python main.py
python -m pytest
python -m ruff check .
```

내가 실행한 명령과 결과:

- 

## 발생한 오류와 원인

오류 메시지를 지우지 말고 그대로 옮긴 뒤, 원인을 한 문장으로 추측해 보세요.

- 오류:
- 원인 추측:
- 해결 후 확인한 것:

## 다음 단계에서 배울 내용

2단계에서는 실제 실거래가 CSV의 컬럼과 데이터 타입을 탐색하고 결측값, 중복, 이상치,
취소 거래를 전처리합니다.

다음 단계 전에 더 확인하고 싶은 것:

- 
